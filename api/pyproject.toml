[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi>=0.100.0", "uvicorn>=0.22.0", "pandas>=2.0", "numpy>=1.24", "scikit-learn>=1.3", "joblib>=1.3", "folium", "reportlab", "onnxruntime" ]
requires-python = "~=3.12.0"
